<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Modbus.Desktop.ViewModels"
             x:Class="Modbus.Desktop.Views.AddDeviceView"
             x:DataType="vm:AddDeviceViewModel">

    <DockPanel Margin="28,20">

        <!-- Header -->
        <Grid DockPanel.Dock="Top" ColumnDefinitions="Auto,*" Margin="0,0,0,20">
            <Button Content="← Back"
                    Command="{Binding GoBackCommand}"
                    Padding="12,8"
                    VerticalAlignment="Center" />
            <TextBlock Grid.Column="1"
                       Text="Add Device"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Margin="20,0"
                       VerticalAlignment="Center" />
        </Grid>

        <!-- Saving progress bar -->
        <ProgressBar DockPanel.Dock="Top"
                     IsIndeterminate="True"
                     IsVisible="{Binding IsSaving}"
                     Height="3"
                     Margin="0,0,0,4" />

        <!-- Main scrollable content -->
        <ScrollViewer>
            <StackPanel Spacing="16">

                <!-- Transport selection -->
                <Border Padding="16"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="#1A1A2E"
                        Background="#1A1A2E">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Transport Type"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#FFFFFF" />
                        <StackPanel Orientation="Horizontal" Spacing="24">
                            <RadioButton Content="RTU (Serial Port)"
                                         IsChecked="{Binding IsRtu, Mode=TwoWay}"
                                         GroupName="Transport" />
                            <RadioButton Content="TCP (Ethernet)"
                                         IsChecked="{Binding IsTcp, Mode=TwoWay}"
                                         GroupName="Transport" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- RTU connection settings -->
                <Border IsVisible="{Binding IsRtu}"
                        Padding="16"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="#1A1A2E"
                        Background="#1A1A2E">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Serial Port Settings"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#FFFFFF" />
                        <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto">

                            <!-- Row 0 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
                                <TextBlock Text="COM Port" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <Grid ColumnDefinitions="*,Auto">
                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding AvailablePorts}"
                                              SelectedItem="{Binding SelectedPort}"
                                              HorizontalAlignment="Stretch" />
                                    <Button Grid.Column="1"
                                            Content="↺"
                                            Command="{Binding RefreshPortsCommand}"
                                            Padding="8,4"
                                            Margin="4,0,0,0"
                                            ToolTip.Tip="Refresh port list"
                                            VerticalAlignment="Center" />
                                </Grid>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Spacing="4" Margin="12,0,0,0">
                                <TextBlock Text="Baud Rate" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <ComboBox ItemsSource="{Binding AvailableBaudRates}"
                                          SelectedItem="{Binding SelectedBaudRate}"
                                          HorizontalAlignment="Stretch" />
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2" Spacing="4" Margin="12,0,0,0">
                                <TextBlock Text="Data Bits" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <NumericUpDown Value="{Binding DataBits}"
                                               Minimum="7" Maximum="8" Increment="1"
                                               FormatString="0"
                                               HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <!-- Row 1 -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Spacing="4" Margin="0,12,0,0">
                                <TextBlock Text="Parity" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <ComboBox ItemsSource="{Binding AvailableParities}"
                                          SelectedItem="{Binding SelectedParity}"
                                          HorizontalAlignment="Stretch" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Spacing="4" Margin="12,12,0,0">
                                <TextBlock Text="Stop Bits" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <ComboBox ItemsSource="{Binding AvailableStopBits}"
                                          SelectedItem="{Binding SelectedStopBits}"
                                          HorizontalAlignment="Stretch" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2" Spacing="4" Margin="12,12,0,0">
                                <TextBlock Text="Address Range" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <NumericUpDown Value="{Binding StartAddress}"
                                                   Minimum="1" Maximum="247" Increment="1"
                                                   FormatString="0"
                                                   Width="90" />
                                    <TextBlock Text="—" VerticalAlignment="Center" Opacity="0.55" />
                                    <NumericUpDown Value="{Binding EndAddress}"
                                                   Minimum="1" Maximum="247" Increment="1"
                                                   FormatString="0"
                                                   Width="90" />
                                </StackPanel>
                            </StackPanel>

                        </Grid>
                    </StackPanel>
                </Border>

                <!-- TCP connection settings -->
                <Border IsVisible="{Binding IsTcp}"
                        Padding="16"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="#1A1A2E"
                        Background="#1A1A2E">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Network Settings"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#FFFFFF" />
                        <TextBlock Text="Discovery uses UDP broadcast (255.255.255.255). Devices on the local network will respond automatically."
                                   FontSize="12"
                                   Opacity="0.6"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <!-- Scan action row -->
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Search Network"
                                Command="{Binding ScanCommand}"
                                Padding="16,8" />
                        <Button Content="Cancel"
                                Command="{Binding CancelScanCommand}"
                                Padding="12,8" />
                        <TextBlock Text="{Binding ScanStatus}"
                                   VerticalAlignment="Center"
                                   Opacity="0.65"
                                   Margin="8,0,0,0" />
                    </StackPanel>
                    <ProgressBar Value="{Binding ScanProgress}"
                                 Maximum="{Binding ScanTotal}"
                                 IsVisible="{Binding IsScanning}"
                                 Height="4" />
                </StackPanel>

                <!-- Scan results -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Found Devices"
                               FontSize="14"
                               FontWeight="SemiBold" />
                    <TextBlock Text="No devices found yet. Run a scan or fill in device details manually below."
                               IsVisible="{Binding !ScanResults.Count}"
                               Opacity="0.5"
                               FontSize="13" />
                    <ListBox x:Name="ScanResultsList"
                             ItemsSource="{Binding ScanResults}"
                             SelectedItem="{Binding SelectedResult}"
                             IsVisible="{Binding ScanResults.Count}"
                             MaxHeight="220">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ScanResultViewModel}">
                                <Grid ColumnDefinitions="*,120,100,100,80" Margin="4,2">
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding DisplayName}"
                                               FontWeight="Medium"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding AddressText}"
                                               FontSize="12"
                                               Opacity="0.65"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding SlaveIdText}"
                                               FontSize="12"
                                               Opacity="0.65"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding SerialText}"
                                               FontSize="12"
                                               Opacity="0.65"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="4"
                                               Text="{Binding FirmwareText}"
                                               FontSize="12"
                                               Opacity="0.65"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <!-- Device details form -->
                <Border Padding="16"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="#1A1A2E"
                        Background="#1A1A2E">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Device Details"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#FFFFFF" />
                        <Grid ColumnDefinitions="*,200,200" RowDefinitions="Auto">
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="Name" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <TextBox Text="{Binding DeviceName}"
                                         Watermark="e.g. KS-3000 #00012345" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="4" Margin="12,0,0,0">
                                <TextBlock Text="Address (1–247)" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <NumericUpDown Value="{Binding SlaveId}"
                                               Minimum="1" Maximum="247" Increment="1"
                                               FormatString="0"
                                               HorizontalAlignment="Stretch" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="4" Margin="12,0,0,0"
                                        IsVisible="{Binding IsTcp}">
                                <TextBlock Text="Device IP" FontSize="11" Opacity="0.55" Foreground="#1F1F1F" />
                                <TextBox Text="{Binding DeviceIp}"
                                         Watermark="e.g. 192.168.1.42" />
                            </StackPanel>
                        </Grid>

                        <!-- Save error -->
                        <TextBlock Text="{Binding SaveError}"
                                   IsVisible="{Binding SaveError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                   Foreground="#D32F2F"
                                   FontSize="13" />
                    </StackPanel>
                </Border>

                <!-- Save row -->
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,20">
                    <Button Content="Save Device"
                            Command="{Binding SaveCommand}"
                            Padding="20,10"
                            FontWeight="Medium" />
                    <ProgressBar IsIndeterminate="True"
                                 IsVisible="{Binding IsSaving}"
                                 Width="100"
                                 Height="4"
                                 VerticalAlignment="Center" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </DockPanel>
</UserControl>
